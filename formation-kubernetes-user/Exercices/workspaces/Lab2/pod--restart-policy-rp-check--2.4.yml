---
apiVersion: v1
kind: Pod
metadata:
  labels:
    app: rpchk
    env: int
  name: rp-check
spec:
  restartPolicy: Always
  terminationGracePeriodSeconds: 2
  containers:
  - image: alpine:3.6
    name: rp-check
    command:
    - "ash"
    - "-c"
    - "sleep 15s"
